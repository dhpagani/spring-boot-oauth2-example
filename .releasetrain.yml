project:
    name: ss0
    type: maven

build: 
    actions: clean build assemble
    
tests:
    - test1:
        actions: test

artifacts:
    - package:
        name: sso.jar
        path: target/